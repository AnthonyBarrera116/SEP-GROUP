// Import Style Sheet form React-Native
import { StyleSheet } from "react-native";

// Style Sheet creation
export default StyleSheet.create({

    // Center view for Sign In page
    centerView: {
        flex:1,
        alignItems:'center',
        justifyContent:'center' 
    },

    // Empasisized text for Sign In
    emphasisText: {
        fontSize: 36,
        color:'blue',
        textAlign: 'center',
    }
    
 });